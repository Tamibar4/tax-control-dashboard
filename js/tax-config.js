/* ============================================================
   tax-config.js - נתוני המס
   זה הקובץ היחיד שצריך לעדכן בתחילת שנת מס חדשה.
   כל הסכומים בשקלים חדשים. כל השיעורים כשבר עשרוני (0.18 = 18%).
   ============================================================ */

window.TAX_CONFIG_DEFAULTS = {

  taxYear: 2026,

  /* התאריך שבו נבדקו הנתונים מול המקור. מוצג במסך ההגדרות. */
  lastUpdated: '2026-08-11',

  /* ---------- מע"מ ---------- */
  vat: {
    rate: 0.18
  },

  /* ---------- מס הכנסה ---------- */
  incomeTax: {

    /* שווי נקודת זיכוי שנתית */
    creditPointValue: 2904,

    /* ברירת מחדל לנקודות זיכוי. ניתן לשינוי במסך ההגדרות.
       2.25 לכל תושב ישראל, ועוד 0.5 לאישה. ילדים, לימודים או עלייה
       מוסיפים עוד, ולכן זה מינימום ולא המספר המדויק. */
    defaultCreditPoints: 2.75,

    /* מדרגות המס השנתיות. upTo = הגבול העליון של המדרגה.
       null = המדרגה האחרונה, בלי גבול עליון. */
    brackets: [
      { upTo:  84120, rate: 0.10 },
      { upTo: 120720, rate: 0.14 },
      { upTo: 228000, rate: 0.20 },
      { upTo: 301200, rate: 0.31 },
      { upTo: 560280, rate: 0.35 },
      { upTo: 721560, rate: 0.47 },
      { upTo:   null, rate: 0.47 }
    ],

    /* מס יסף - 3% נוספים על ההכנסה שמעל הסף.
       ניתן לכבות במסך ההגדרות. */
    surtax: {
      enabled: true,
      threshold: 721560,
      rate: 0.03
    },

    /* שיעור מקדמות אישי שנקבע בתיק, אם נקבע.
       0 = לא נקבע שיעור, המערכת לא משתמשת בו. */
    defaultAdvanceRate: 0
  },

  /* ---------- ביטוח לאומי וביטוח בריאות ---------- */
  /* מחושב לפי הרווח החודשי הממוצע מהעסק. */
  bituahLeumi: {
    lowCeiling: 7703,    /* עד הסכום הזה - השיעור המופחת */
    lowRate: 0.077,
    highCeiling: 51910,  /* מעל הסכום הזה לא משלמים */
    highRate: 0.18,
    defaultMonthlyAdvance: 270
  },

  /* ---------- ניסוח קבוע ---------- */
  disclaimer: 'תחזית לצורכי תזרים בלבד. יש לבדוק מול רו"ח או יועץ מס.'
};
